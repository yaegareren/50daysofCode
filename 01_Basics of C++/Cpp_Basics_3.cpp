// CONDITIONS, CONDITIONAL STATEMENTS, LOOPS/INFINITE LOOPS, TEST CASES

#include <bits/stdc++.h>
using namespace std; // to get cout and cin
int main()

{
  // discussion of if, else, else if.

  /* variables declared inside a scope can be used inside that scope
  only. They are called local variables. Global variables declared
  before int main and they can be used anywhere in the program. */

  // loops:

  /* syntax of while loop:

  while(condition){

    ...code
  }

 example:

  int i=1;
  while(i<=10){
    cout<<i<<endl;
    i++;
  } */

  /* syntax of for loop:

  for(initialisation;condition;operations){

    ...code
  }

  example:

  for(int i=1;i<=10;++i)
  {
    cout<<i<<endl;
  } */

  /* int n;
  cin>>n;
  for(int i=0;i<=n;++i){
    for (int j = 1; j <= i; ++j)
    {
      cout<<"*";
    }

    cout<<endl; */

  /* how to put test cases into program:

   while(t--){
     ....code
   } */
}
